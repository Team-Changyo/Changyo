import { styled } from 'styled-components';

export const AccountPageLayoutContainer = styled.div`
	position: relative;
	margin-top: 44px;

	.account-summary {
		padding: 2rem 0;
	}

	.account-filter {
		margin-bottom: 1rem;
	}

	.account-list {
	}
`;
