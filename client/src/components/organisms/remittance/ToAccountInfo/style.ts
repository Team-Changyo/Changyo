import { styled } from 'styled-components';

export const ToAccountInfoContainer = styled.div`
	display: flex;
	flex-direction: column;
	gap: 5px;
	color: var(--gray-500);

	.account-holder-name {
		color: var(--main-color);
	}
`;
