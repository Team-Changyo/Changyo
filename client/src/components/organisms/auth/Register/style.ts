import { styled } from 'styled-components';

export const RegisterContainer = styled.div`
	position: relative;
	height: calc(100vh - 4rem);

	.next-btn {
		position: absolute;
		width: 100%;
		bottom: 2rem;
	}
`;
