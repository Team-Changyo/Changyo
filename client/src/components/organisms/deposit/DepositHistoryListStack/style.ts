import { styled } from 'styled-components';

export const DepositHistoryListStackContainer = styled.div`
	display: flex;
	flex-direction: column;
	gap: 2rem;

	.return-wait,
	.return-done {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}
`;
